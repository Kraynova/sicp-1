процедура (define (p) (p)) - уйдет в рекурсию.

Поэтому, если интерпретатор работает аппликативно - он уйдет в рекурсию, из-за вычисления всех подвыражений(операндов) 
перед применением  процедуры(оператора).

Если работает с нормальным порядком вычислений, то он только подставит выражения на место формальных параметров в теле процедуры.
 И будет вычислять только необходимый для конструкции if параметр, равный 0.
